//include ':annotation'
//include ':compiler'
include ':app'
include ':utils'
include ':base'

//将 Flutter 模块作为子项目添加到

setBinding(new Binding([gradle: this]))
evaluate(new File(
        settingsDir.parentFile,
        'myfluttermodule/.android/include_flutter.groovy'
))
